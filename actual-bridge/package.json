{
  "name": "actual-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny REST bridge that runs @actual-app/api in a normal Node runtime and exposes normalized account JSON over HTTPS. Runs on the Actual host (Hetzner), not on Vercel.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "@actual-app/api": "^26.6.0",
    "express": "^4.21.2"
  }
}
